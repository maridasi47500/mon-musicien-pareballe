<%= form_with(model: musicalinstrument) do |form| %>
  <% if musicalinstrument.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musicalinstrument.errors.count, "error") %> prohibited this musicalinstrument from being saved:</h2>

      <ul>
        <% musicalinstrument.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
